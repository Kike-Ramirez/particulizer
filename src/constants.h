#ifndef CONSTANTS_H
#define CONSTANTS_H

namespace Constants
{
    const int OUTPUT_WIDTH = 1280;
    const int OUTPUT_HEIGHT = 1024;
    const float ASPECT_RATIO = float(OUTPUT_WIDTH) / float(OUTPUT_HEIGHT);

}
#endif // CONSTANTS_H
